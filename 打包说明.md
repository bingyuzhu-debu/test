# 银行对账单生成系统 - 打包说明

## 📦 打包成exe文件

### 方法一：在Windows电脑上打包（推荐）

#### 前置要求
- Windows 7/10/11
- Python 3.9 或更高版本
- 至少 500MB 可用磁盘空间

#### 打包步骤

1. **将整个项目文件夹复制到Windows电脑**

2. **双击运行 `build_windows.bat`**
   - 脚本会自动检查环境
   - 自动安装所有依赖
   - 自动执行打包
   - 完成后生成 `dist/银行对账单生成系统.exe`

3. **打包完成后**
   - exe文件位于 `dist/` 目录
   - 双击exe即可启动
   - 会自动打开浏览器

#### 手动打包（如果自动脚本失败）

```bash
# 1. 安装依赖
cd backend
pip install -r requirements.txt
cd ..

# 2. 安装PyInstaller
pip install pyinstaller

# 3. 执行打包
pyinstaller build_exe.spec --clean --noconfirm
```

### 方法二：使用虚拟机或远程Windows

如果你只有Mac，可以：
1. 使用Parallels/VMware安装Windows虚拟机
2. 在虚拟机中按方法一打包
3. 将生成的exe文件复制出来

### 方法三：使用GitHub Actions自动打包（高级）

可以配置GitHub Actions在云端Windows环境中自动打包。

---

## 🔧 打包配置说明

### 核心文件

1. **launcher.py** - 启动器
   - 自动启动FastAPI服务
   - 自动打开默认浏览器
   - 处理打包后的路径

2. **build_exe.spec** - PyInstaller配置
   - 指定要打包的文件
   - 配置隐藏导入
   - 设置exe名称和图标

3. **build_windows.bat** - Windows打包脚本
   - 一键完成所有打包步骤

### 打包后的目录结构

```
dist/
└── 银行对账单生成系统.exe
    ├── backend/
    │   ├── uploads/      # 上传文件目录
    │   ├── outputs/      # 生成文件目录
    │   └── *.py          # 后端代码
    ├── frontend/
    │   ├── index.html
    │   └── assets/
    └── 底表文件夹/
```

---

## 📝 使用说明

### 启动应用

1. **双击 `银行对账单生成系统.exe`**
2. 等待3-5秒，控制台显示启动信息
3. 浏览器自动打开 `http://localhost:8000`
4. 开始使用系统

### 停止应用

- 关闭控制台窗口
- 或按 `Ctrl + C`

### 首次运行

首次运行可能需要：
- 初始化数据库（自动）
- 创建必要目录（自动）
- 大约需要5-10秒

---

## ⚠️ 常见问题

### Q1: 打包失败，提示找不到模块

**解决方案：**
```bash
# 确保所有依赖都已安装
pip install -r backend/requirements.txt
pip install pyinstaller
```

### Q2: exe运行时闪退

**解决方案：**
1. 在命令行中运行exe，查看错误信息
2. 检查是否缺少依赖文件
3. 确认端口8000未被占用

### Q3: 浏览器没有自动打开

**解决方案：**
手动访问 `http://localhost:8000`

### Q4: 提示端口被占用

**解决方案：**
```bash
# Windows
netstat -ano | findstr :8000
taskkill /PID <进程ID> /F

# 或修改launcher.py中的端口号
```

### Q5: 打包后体积太大（>100MB）

这是正常的，因为包含了：
- Python运行时
- 所有依赖库
- 前后端代码
- 可以使用UPX压缩（在spec文件中已启用）

---

## 🎨 自定义配置

### 更改应用图标

1. 准备一个 `.ico` 文件
2. 修改 `build_exe.spec` 中的 `icon` 参数：
   ```python
   icon='your_icon.ico'
   ```

### 更改应用名称

修改 `build_exe.spec` 中的 `name` 参数：
```python
name='你的应用名称'
```

### 隐藏控制台窗口

如果不想显示控制台，修改 `build_exe.spec`：
```python
console=False  # 改为False
```

**注意：** 隐藏控制台后，无法看到错误信息，建议调试完成后再隐藏。

---

## 📊 打包文件大小优化

### 当前配置已启用的优化

- ✅ UPX压缩
- ✅ 排除不必要的模块
- ✅ 单文件模式

### 进一步优化（可选）

1. **使用更小的Python发行版**
   ```bash
   # 使用embeddable版本的Python
   ```

2. **移除不需要的依赖**
   - 如果不需要PDF导出，移除reportlab
   - 如果不需要CSV导出，相关代码可简化

---

## 🚀 发布建议

### 打包完成后的发布流程

1. **测试exe**
   - 在全新的Windows电脑上测试
   - 确认所有功能正常
   - 检查文件生成路径

2. **准备发布包**
   ```
   银行对账单生成系统_v1.0/
   ├── 银行对账单生成系统.exe
   ├── 使用说明.txt
   ├── 底表文件夹/（示例文件）
   └── README.md
   ```

3. **编写用户文档**
   - 系统要求
   - 安装步骤
   - 使用教程
   - 常见问题

---

## 🔄 版本更新

### 更新打包版本

1. 修改 `backend/main.py` 中的版本号
2. 重新运行打包脚本
3. 测试新版本
4. 发布更新

### 版本命名建议

```
银行对账单生成系统_v1.0.0_20251225.exe
```

包含：版本号 + 日期

---

## 📧 技术支持

如果打包过程遇到问题，请检查：

1. Python版本是否 >= 3.9
2. 所有依赖是否正确安装
3. PyInstaller是否最新版本
4. Windows防火墙/杀毒软件是否拦截

**获取详细日志：**
```bash
pyinstaller build_exe.spec --clean --noconfirm --log-level DEBUG
```

---

## ✅ 打包检查清单

打包前确认：
- [ ] 所有功能在开发环境测试通过
- [ ] requirements.txt包含所有依赖
- [ ] build_exe.spec配置正确
- [ ] 前端文件完整
- [ ] 数据库初始化正常

打包后确认：
- [ ] exe可以正常启动
- [ ] 浏览器自动打开
- [ ] 上传文件功能正常
- [ ] 生成对账单功能正常
- [ ] 下载文件功能正常
- [ ] 历史记录查询正常

---

**最后更新：** 2025-12-25
**适用版本：** v1.0.0
